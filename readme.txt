The linuxKvacModified are source files that should work on linux, it also has a file with a compile command that should work with g++.
The project was built in VS2022, for some reason the micro-exx library will not upload to git, so extract the micro-ecc.zip into the lib folder of VS if you want
to use it in VS.